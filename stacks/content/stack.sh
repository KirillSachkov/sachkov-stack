# Стек: контент (уроки, видео, доски, вики, проза)
#
# Компилировать нечего, поэтому инкрементальный гейт пустой. Качество здесь проверяется
# не сборкой, а визуальным QA и правилами из stacks/content/rules/.
#
# Функция определена намеренно, а не пропущена: так stop-гейт видит подключённый стек и
# не считает, что харнес недоделан.

stack_incremental_check() {
  cat > /dev/null
  return 0
}

stack_lint_file() {
  return 0
}
