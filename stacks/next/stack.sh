# Стек: Next.js — надстройка над node.
# Проверки те же (tsc по проекту, eslint по файлу), отдельная папка нужна ради своих
# правил в next/rules/. Дублировать логику незачем.
. "$(dirname "${BASH_SOURCE[0]}")/../node/stack.sh"
